# 설계 요약

## 간단한 설계 내용
- 기존 공격 카드 구조는 유지하고, `SQL Injection / XSS`, `비정상 스캐닝 / 봇 요청`, `Origin 직접 접근` 3개 시나리오에만 결과 증거 영역을 추가하는 방식으로 설계
- 각 공격은 기존처럼 `취약 환경`과 `보안 환경`을 동시에 실행하고, 실행 결과 아래에서 이 공격에 맞는 세부 증거를 바로 확인할 수 있도록 구성
- 공통 레이아웃을 크게 바꾸지 않고 `AttackCard` 내부에 시나리오별 증거 패널만 조건부로 렌더링하도록 구현

## 설계 의도
- 실습자가 단순히 `차단됨 / 도달함`만 보는 것이 아니라, 실제로 무엇이 확인됐는지를 바로 이해할 수 있도록 하는 목적
- 브루트포스의 자격증명 확인, S3 탈취의 취약 파일 확인처럼 새 공격 항목도 각자에 맞는 결과 증거를 제공하는 방향으로 통일
- 공격 의도 자체를 흐리지 않고, 각 시나리오의 핵심 메시지를 더 직관적으로 전달하는 데 초점

## 그렇게 설계한 이유
- `SQL Injection / XSS`는 어떤 패턴이 앱까지 갔고 어떤 패턴이 WAF에서 막혔는지를 보여줘야 실습 효과가 분명해지기 때문
- `비정상 스캐닝 / 봇 요청`은 어떤 경로가 원본까지 도달했고 어떤 경로가 앞단에서 소거됐는지를 보여줘야 공격 표면 차이가 잘 드러나기 때문
- `Origin 직접 접근`은 어떤 원본 주소를 직접 요청했고 어떤 응답이 왔는지를 보여주는 것이 원본 노출 여부를 가장 분명하게 설명할 수 있기 때문
- 반대로 실제 스크립트 실행 화면이나 외부 새 창 열기 같은 기능은 공격 의도를 흐리거나 UI를 불필요하게 복잡하게 만들 수 있어 추가하지 않음

## 적용 내용
- `SQL Injection / XSS`: 패턴별 요청 결과 표 추가
- `비정상 스캐닝 / 봇 요청`: 경로별 원본 도달 여부와 요약 정보 추가
- `Origin 직접 접근`: 직접 접근 대상 주소, 응답 상태, 지연 시간, 복사 기능 추가

## 적용 효과
- 실습자는 각 공격에서 `무엇이 실제 결과였는지`를 로그 해석 없이 바로 확인 가능
- 취약 환경과 보안 환경의 차이를 더 빠르게 이해 가능
- 기존 구조를 유지한 채 필요한 부분만 보강해 UI 복잡도는 크게 늘리지 않음

## 패턴과 경로를 미리 지정한 이유
- `SQL Injection / XSS`의 6개 패턴은 SQLi와 XSS를 모두 포함하면서도, 사람이 봤을 때 의미가 분명하고 WAF나 입력 필터가 반응하기 쉬운 대표 문자열로 구성함
- `비정상 스캐닝 / 봇 요청`의 6개 경로는 `/admin`, `/wp-login.php`, `/.env`, `/phpmyadmin`, `/server-status`, `/config.bak`처럼 실제 자동 스캐너가 자주 탐색하는 전형적인 경로로 구성함
- 이렇게 대표 샘플을 고정하면 실습자가 각 요청의 의미를 바로 이해할 수 있고, 같은 순서와 같은 입력으로 취약 환경과 보안 환경을 안정적으로 비교할 수 있음
- 즉 현재 구조는 임의의 입력을 실시간으로 탐지해 분류하는 시스템이라기보다, 설명력이 높은 대표 공격 입력을 정해두고 그 결과 차이를 보여주는 비교형 시뮬레이션에 가까움

## 미리 지정하지 않으면 어려운 점
- 현재 구현은 결과를 동적으로 해석하는 방식이 아니라, 미리 정의한 패턴 배열과 경로 배열의 순서를 기준으로 결과 표를 렌더링함
- 그래서 지금은 `1번 결과는 어떤 패턴`, `2번 결과는 어떤 경로`인지가 이미 정해져 있어야 UI에서 패턴별 표와 경로별 표를 자연스럽게 구성할 수 있음
- 현재 결과 타입에는 `patternName`, `payload`, `path` 같은 필드가 없고, 주로 `attempt`, `status`, `latency`, `blocked`, `label`만 저장되므로, 고정 목록 없이 임의 입력을 받아도 그것이 정확히 어떤 패턴/경로였는지 구조적으로 추적하기 어려움
- 무엇이 취약을 유발했는가는 HTTP 응답만 보고 항상 확정할 수 없음. 예를 들어 SQLi/XSS 페이로드를 바꿔도 앱이 같은 `200`, `401`, `404`를 줄 수 있고, WAF도 여러 입력을 같은 `403`으로 막을 수 있으므로, 응답만으로는 `진짜 취약을 유발한 패턴`과 `그냥 도달만 한 패턴`을 구분하기 어려움
- 봇 경로도 마찬가지로 `/admin`이 `404`여도 그것이 원본까지 도달한 뒤의 `404`인지, 아니면 엣지 계층에서 막힌 `404/403`인지 단순 응답만으로는 구분하기 어려움. 이를 분명히 하려면 원본 로그, CloudFront/WAF 로그, 애플리케이션 로그 같은 추가 근거가 필요함
- 완전히 동적으로 하려면 서버 이벤트와 결과 데이터에 패턴명, 실제 payload, 스캔 경로를 함께 담고, 프론트엔드도 고정 배열이 아니라 서버가 돌려준 항목 목록을 기준으로 표를 그리도록 바꿔야 함

## 정리
- 지금처럼 미리 지정하면 재현성과 설명력이 높아지고, 실습 시나리오의 메시지가 명확해짐
- 반대로 미리 지정하지 않아도 이론적으로는 가능하지만, 현재 구조만으로는 어떤 입력이 실제로 취약을 유발했는지 자동으로 깔끔하게 분류해서 보여주기 어려움
- 따라서 현재 구현은 `대표 공격 입력을 고정해 비교하는 구조`로 이해하는 것이 가장 정확함

## Evidence 개선 후 읽는 법
- `SQL Injection / XSS` evidence는 단순히 성공/실패만 보는 표가 아니라, 취약 환경은 `보호 계층 없이 원본 앱으로 직접 시도하는 흐름`, 보안 환경은 `CloudFront/WAF를 거쳐 앞단에서 판정되는 흐름`으로 해석하도록 구성함
- 따라서 같은 연결 실패가 나오더라도 취약 환경은 `원본 직접 노출 관점의 실패`, 보안 환경은 `앞단 보호 경유 후 비교가 멈춘 상태`로 읽어야 하며, 두 환경의 의미를 다르게 전달하는 것이 핵심
- 이 evidence를 통해 `어떤 패턴이 취약 환경에서는 앱 도달 가능성이 있는지`, `보안 환경에서는 어떤 패턴이 WAF 차단 또는 앞단 보호 흐름에 놓이는지`, `현재 결과가 공격 비교인지 연결 문제인지`를 함께 판단할 수 있음

- `비정상 스캐닝 / 봇 요청` evidence는 취약 환경을 `숨은 경로 요청이 원본으로 바로 향하는 시나리오`, 보안 환경을 `CloudFront 같은 앞단 보호 계층을 먼저 거치는 시나리오`로 분리해서 읽도록 구성함
- 그래서 같은 연결 실패가 보여도 취약 환경은 `원본 직접 탐색 시도 실패`, 보안 환경은 `앞단 경유 후 원본 연결 확인 실패`로 해석되며, 오른쪽 요약 카드도 `원본 도달`, `앞단 차단`, `연결 실패`, `미구성`, `대기/진행 중`을 나눠 보여주도록 정리함
- 이 evidence를 통해 `정찰 경로가 원본까지 직접 닿는 구조인지`, `보안 환경에서 앞단이 먼저 흡수/차단하는지`, `현재 결과가 실제 방어 차이인지 아니면 환경 연결 문제인지`를 더 분명하게 확인할 수 있음
