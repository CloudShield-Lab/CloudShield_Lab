**1) 프로젝트 수행 과정**

(목적 정의/전체 수행 흐름/수행 과정 요약)

- 프로젝트 목적

`클라우드 보안 설정이 실제 공격에 얼마나 큰 차이를 만드는지 눈으로 직접 확인할 수 있는 보안 임팩트 시뮬레이터`

- 전체 수행 흐름

1. 동일한 서비스 코드를 기준으로 `취약 환경`과 `보안 환경` 두 가지 AWS 구조를 설계
2. 수동 배포용 Guide를 작성해 사용자가 인프라를 직접 구축하며 구조를 이해할 수 있도록 함
3. Terraform을 이용해 취약 환경과 보안 환경을 자동으로 `구축 / 삭제`할 수 있는 자동 배포 흐름을 구현
4. Attack Simulator에서 동일한 공격 요청을 두 환경에 동시에 보내고, 결과 차이를 비교하도록 구성
5. 공격 결과를 `실시간 아키텍처 변화`와 `Server-Sent Events 로그 타임라인`으로 시각화
6. 공격 과정에서 기록된 `로우 레벨 로그`와 세션 데이터를 저장하고, Amazon Bedrock 기반 사후 분석 기능으로 결과 해석까지 연결
7. 최종적으로 사용자는 인프라 보안 설정 차이가 실제 공격 흐름과 결과에 어떤 영향을 주는지 한 화면에서 학습할 수 있게 됨

- 수행 과정 요약

- 설계 단계: 취약 환경은 직접 노출 구조, 보안 환경은 CloudFront + WAF + 제한된 접근 구조로 분리
- 구축 단계: 수동 배포와 Terraform 자동 배포를 모두 지원
- 실행 단계: 브루트포스, S3 접근, Rate Limit 등 공격 시나리오 비교
- 시각화 단계: 아키텍처 패널과 타임라인으로 실시간 변화 표시
- 분석 단계: 저장된 로그를 활용해 Amazon Bedrock으로 사후 분석 수행

---

**2) 아키텍처 설계**

- 아키텍처 설계

![image.png](attachment:a1184014-5992-4a4b-9a7f-e3b5fc6f3e52:image.png)

- 핵심 설계 포인트
    - 비교 실습 중심 설계: 같은 서비스 기능을 취약 환경과 보안 환경으로 나눠 인프라 보안 차이만 눈에 보이게 비교
    - 보안 계층 분리 설계: 보안 환경은 CloudFront와 WAF를 앞단에 두고, 원본 서버는 직접 노출을 줄이는 형태
    - 접근 경로 최소화: EC2 보안 그룹을 CloudFront 경유 중심으로 제한하고, S3는 퍼블릭 대신 제한된 접근 구조로 설계
    - 운영 보안 내재화: IAM, Secrets Manager, KMS, SSM을 통해 권한, 비밀정보, 운영 접근을 분리
    - 관제 확장형 구조: Wazuh, CloudTrail, CloudWatch, Security Hub, GuardDuty, Config, Inspector 같은 서비스가 오른쪽에 별도 보안 관제 계
    층으로 배치돼 있음
    - 자동화 포함 설계: Terraform과 GitHub Actions가 단순 배포가 아니라 반복 실습 가능한 환경 재생성 수단 역할
    - 분석까지 닫히는 구조: 공격 실행으로 끝나지 않고 로그 수집, 중앙 분석, AI 해석까지 이어지는 점이 이 아키텍처의 강점

- SentinelShare AWS 기능 정리

**1. `CloudFront`**
- 보안 환경의 인터넷 진입점 역할
- 사용자 요청이 바로 EC2로 들어가지 않고 CloudFront를 먼저 거치도록 구성
- 정적 프론트엔드는 S3와 연결하고, `/api` 요청은 백엔드로 전달하는 구조로 사용
- 외부 요청을 엣지 계층에서 먼저 제어할 수 있고, 보안 환경의 구조적 차이를 가장 직관적으로 보여주는 핵심 요소

**2. `AWS WAF`**
- 악성 요청과 과도한 요청을 앞단에서 차단하는 웹 방화벽
- Rate-based rule, Managed Rules를 적용해 브루트포스 로그인, 반복 요청, 악성 패턴 요청이 애플리케이션에 도달하기 전에 차단되도록 구성
- 취약 환경과 보안 환경의 차이를 가장 명확하게 보여주며, 같은 공격이라도 보안 환경에서는 어디서 막히는지 시각적으로 확인 가능

**3. `Amazon S3`**
- 파일 저장과 프론트엔드 정적 배포에 사용되는 스토리지
- 취약 환경은 퍼블릭 버킷, 보안 환경은 Block Public Access가 적용된 프라이빗 버킷으로 구성
- 공격 결과 세션 데이터 저장에도 S3를 사용
- 공개 버킷과 프라이빗 버킷 차이를 실습으로 직접 보여줄 수 있고, 사후 분석 데이터 저장소 역할도 수행

**4. `Amazon EC2`**
- 취약 환경과 보안 환경의 애플리케이션 서버 역할
- 동일한 백엔드 코드를 두 환경에 각각 배치해 인프라 보안 계층 차이만 비교할 수 있도록 구성
- 보안 환경은 Security Group 제한과 CloudFront 경유 접근 구조 적용
- 같은 서비스 코드라도 보안 설정에 따라 결과가 얼마나 달라지는지 비교 가능

**5. `Amazon Bedrock`**
- 공격 세션 결과를 사후 분석하는 AI 서비스
- 저장된 세션 로그, 차단율, 응답시간, 시나리오 정보를 바탕으로 공격 영향과 방어 효과를 요약·분석
- 단순 성공/실패 비교를 넘어서 결과를 해석 가능한 보고 형태로 바꿔주며, 교육용·발표용 활용도가 높음

**6. `AWS Secrets Manager`**
- 민감한 비밀값을 코드 밖에서 안전하게 관리하는 서비스
- DB 비밀번호, JWT Secret 등을 평문 환경변수 대신 Secret으로 분리 관리
- 시크릿 노출 위험 감소, 운영 환경에 더 가까운 구조 구현

**7. `AWS KMS`**
- 시크릿과 데이터 암호화를 위한 키 관리 서비스
- Secrets Manager에 저장되는 민감정보를 암호화하는 키로 사용
- 암호화된 시크릿 관리 구조 구현

**8. `IAM Role / Policy / Instance Profile`**
- 각 리소스가 필요한 권한만 갖도록 제어하는 권한 관리 기능
- EC2가 S3, Secrets Manager, KMS, ECR, SSM에 접근할 수 있도록 최소 권한 정책을 분리 적용
- 보안 환경의 실제 운영 구조를 더 잘 반영하고 권한 남용 가능성을 줄임

**9. `Systems Manager (SSM)`**
- EC2를 안전하게 관리하고 명령을 원격 실행하는 기능
- Terraform 배포 후 DB 마이그레이션이나 초기 설정을 SSM으로 실행하고, SSH 직접 노출을 줄이는 방식으로 활용
- 운영 접근 통제를 강화하고 자동화된 서버 초기화 흐름을 지원

**10. `OIDC 기반 IAM Federation`**
- GitHub Actions가 장기 AWS 키 없이 AWS Role을 Assume 하도록 하는 인증 방식
- 배포 워크플로에서 AWS 자격증명을 직접 저장하지 않고 OIDC로 인증
- CI/CD 보안 수준 향상, 장기 Access Key 관리 부담 감소

그외
- `VPC`: 취약 환경과 보안 환경의 네트워크 분리
- `Subnet`: 인스턴스가 배치될 네트워크 구간 구성
- `Internet Gateway`: 외부 인터넷 연결 제공
- `Route Table`: 외부와 내부 라우팅 경로 정의
- `Elastic IP`: EC2에 고정 공개 주소 부여
- `Security Group`: 포트와 접근 IP 범위를 제어
- `Origin Access Control (OAC)`: CloudFront를 통한 S3 접근만 허용
- `S3 Bucket Policy`: 버킷 접근 주체와 경로를 세부적으로 제한
- `Block Public Access`: S3 공개 접근 차단
- `Presigned URL`: 제한된 시간 동안만 파일 접근 허용
- `Amazon ECR`: 컨테이너 이미지를 저장하고 EC2/ECS가 pull
- `Amazon ECS Fargate`: Attack Dashboard를 컨테이너로 운영
- `CloudWatch Logs`: ECS 및 운영 로그 저장
- `WAF Metrics / Sampled Requests`: WAF 차단 결과와 요청 지표 확인
- `AWS CLI`: 배포·설정 자동화에 사용
- `Terraform S3 Backend`: Terraform state를 S3에 저장
- `CloudFront Invalidation`: 프론트엔드 변경 후 캐시 초기화

우리 프로젝트에서 가장 핵심적인 AWS 기능 :
`CloudFront`, `WAF`, `S3`, `EC2`, `Secrets Manager`, `KMS`, `IAM`, `SSM`, `Bedrock`

이 중 프로젝트의 차별성 3개
- `CloudFront + WAF`: 공격이 앞단에서 어떻게 차단되는지 비교
- `S3 Public vs Private`: 데이터 접근 경로 차이 비교
- `Amazon Bedrock`: 저장된 공격 결과를 사후 분석으로 확장

C:\Users\User\Desktop\AWS Guide\Sentinel_Share 이 경로의 파일을 자세히 파악하고
C:\Users\User\Desktop\AWS Guide\image.png 이 파일도 자세히 파악해 해당 아키텍처로 알 수 있는 흐름과 설계 포인트를 정리해서 알려줘 

---

**3) 활용 기술 / 구현 방법 / 핵심 기능 / 구현 결과**

- 활용 기술

- Infrastructure: Terraform, AWS CLI, GitHub Actions
- AWS: EC2, S3, CloudFront, WAF, Security Group, Secrets Manager, PostgreSQL
- Frontend / Dashboard: Next.js, React, Tailwind CSS
- Visualization / Streaming: React Flow, Server-Sent Events
- Post Analysis: S3 세션 저장, Amazon Bedrock

- 구현 방법

- 동일한 백엔드 코드를 취약 환경과 보안 환경에 각각 배치하여 비교 기준을 고정
- `InfraControl`을 통해 Terraform workflow를 호출하여 인프라를 자동 구축 / 삭제하도록 구성
- 공격 시뮬레이션 결과를 이벤트 단위로 가공하여 `CloudFront -> WAF -> EC2 -> Service Logic -> S3` 흐름에 매핑
- SSE 로그를 이용해 아키텍처 패널과 타임라인을 동시에 갱신하도록 구현
- 공격 실행 후 세션 데이터를 S3에 저장하고, Amazon Bedrock으로 분석 결과를 생성하도록 구현

- 핵심 기능
    
    (설명/구현 방법/기대효과)
    

1. **Terraform 기반 인프라 자동 구축 / 삭제**
- 설명: 취약 환경과 보안 환경을 Terraform으로 반복 배포하고 제거하는 기능
- 구현 방법: 대시보드에서 apply / destroy를 실행하면 GitHub Actions 기반Terraform 워크플로가 동작하고, 진행 상태를 SSE로 전달함. 배포 완료 후 backend URL, frontend URL, bucket 정보까지 자동 반영
- 기대 효과 : 같은 실습 환경을 빠르게 다시 만들 수 있어 재현성 확보, 수동 설정 오 류 감소, 반복 실습과 데모에 유리
2. **실시간 아키텍처 변화 + SSE 로그 타임라인**
- 설명: 공격 요청이 어느 계층까지 도달했는지와 어디서 차단됐는지를 실시간으로 보 여주는 기능
- 구현 방법 : 공격 API가 start, stage, result, complete 이벤트를 SSE로 전송하고,
이를 아키텍처 패널과 타임라인에 동시에 반영함. CloudFront -> WAF -> EC2 -> Service Logic -> S3 흐름 기준 비교
- 기대 효과 : 단순 성공·실패가 아니라 차단 지점과 도달 경로를 바로 이해 가능, 교육과 발표에서 전달력 강화
3. **세션 단위 요청 로그 저장 + Amazon Bedrock 사후 분석**
- 설명: 공격 실행 결과와 요청별 로그, 메트릭을 저장하고 AI로 사후 분석하는 기능
- 구현 방법 : 공격 종료 후 sessionId 기준 세션 데이터를 S3에 저장하고, 분석 화면 에서 세션 목록과 원본 로그를 불러온 뒤 Bedrock으로 차단율, 응답시간, 공격 영향, 방어 효과를 분석함
- 기대 효과 : 실시간 비교에 그치지 않고 결과를 기록과 분석 자산으로 활용 가능, 보고서·발표·학습 자료로 확장 가능

- 구현 결과

- 취약 환경은 공격 요청이 내부 서비스까지 직접 도달하는 흐름을 확인 가능
- 보안 환경은 CloudFront와 WAF가 앞단에서 요청을 제어하여 조기 차단 효과를 보여줌
- 사용자는 같은 공격이라도 보안 설정 차이에 따라 도달 경로와 결과가 달라지는 것을 시각적으로 확인할 수 있음
- 실시간 시각화와 사후 분석을 함께 제공해 단순 시연이 아닌 학습형 보안 플랫폼 형태로 결과물을 완성

---

**4) 피드백 내용과 적용(보완) 내용**

(피드백/보완 내용 및 결과)

- 피드백 1. AWS 보안 기능을 더 활용할 필요가 있음

- 보완 내용:

- 피드백 2. 아키텍처 시각화와 타임라인 로그를 더 직관적으로 보여줄 필요가 있음

- 보완 내용:
  - 공격 대상 서버에 대한 아키텍처 변화를 실시간으로 표현하는 시각화 패널을 구현
  - SSE(Server-Sent Events) 로그를 활용해 공격 흐름과 차단 지점을 타임라인으로 실시간 확인할 수 있도록 개선
  - 사용자가 단순 응답 코드가 아니라 "어느 계층에서 막혔는지"를 바로 이해할 수 있게 보완

- 피드백 3. 대시보드 상에서 전체적인 흐름과 목적을 파악하기 어려움

- 보완 내용:
  - 메인 페이지를 별도로 생성하여 프로젝트 목적과 전체 실습 흐름이 먼저 보이도록 구성
  - 사용자가 수동 배포 / 자동 배포 / 공격 시뮬레이션 / 분석 기능으로 어떻게 이동하는지 한눈에 이해할 수 있도록 구조를 정리
  - 이를 통해 대시보드 진입 단계에서 프로젝트 의도와 사용 흐름을 더 쉽게 파악할 수 있도록 개선

- 피드백 적용 결과

- 프로젝트는 단순 비교 시연을 넘어 `확장 가능한 AWS 보안 구조`, `실시간 아키텍처 시각화`, `명확한 메인 진입 흐름`을 갖춘 형태로 발전
- 그 결과 SentinelShare는 클라우드 보안 설정 차이를 사용자가 처음 진입하는 순간부터 실시간 비교와 결과 해석까지 직관적으로 이해할 수 있는 보안 임팩트 시뮬레이터로 완성